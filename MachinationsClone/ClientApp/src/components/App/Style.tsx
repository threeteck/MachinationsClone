import {styled} from "@mui/material";


const Wrapper = styled('div')({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: '16px',
});

export const Styled = {
    Wrapper,
};
